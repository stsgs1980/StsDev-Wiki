# Правила

**Обязательные правила для всех проектов экосистемы**

---

## R-01: Не читал — не утверждаешь

→ Полный текст принципа: [Анти-галлюцинация](principles/anti-hallucination.md)

Суть: любое утверждение о коде, файлах или структуре проекта должно быть основано на реальном чтении файлов в текущей сессии. Память прошлых сессий = гипотеза, не факт.

## R-02: Анти-монолит

→ Детали стандарта: [S-01 Monorepo](standards.md), [S-02 Feature-based](standards.md)

Принцип: мелкие файлы, чёткие границы, нет god objects. Для Next.js/React — конкретные лимиты (файл ≤ 150 строк, useState ≤ 3) проверяются `eslint-plugin-3a`. Для других проектов — та же идея без конкретных лимитов.

## R-03: Коммит — осмысленный

Коммит без описания или с UUID вместо сообщения = мусор. Правило: коммит без осмысленного сообщения = не принимается.

## R-04: Build должен проходить

Для Next.js: `next build` и `tsc --noEmit` — без ошибок перед коммитом.
Для других проектов — аналог: lint/typecheck/test должны проходить.

## R-05: Хардкод цветов — через токены

Новый код — через design tokens (@stsgs/ui), не через hex. Брендовые цвета (Google, etc.) — исключение.

## R-06: Язык

- Код, UI, коммиты, README = English
- Wiki, обсуждения = Русский
- См. [decision: language-strategy](decisions/language-strategy.md)

## R-07: Конфликты toolkit решаем в toolkit

Zai-agent-toolkit = read-only reference. Проекты используют его как источник стандартов, не встраивают.

## R-08: Пакеты — однонаправленные зависимости

```
app → @stsgs/ui → @stsgs/shared
app → @stsgs/prompting → @stsgs/shared
eslint-plugin-3a → (standalone)
```

Обратных зависимостей нет. Shared = фундамент.

---

См. также:
- [Принципы](principles/anti-hallucination.md) — вечные правила (почему R-01, R-03, R-04 существуют)
- [Стандарты](standards.md) — детальные стандарты кода (как R-02 реализуется)
- [Протокол начала сессии](session-protocol.md) — что делать при старте
- [Протокол нового проекта](new-project-protocol.md) — при создании репо

> **Иерархия:** принципы (почему) → правила (что) → стандарты (как). Каждое правило ссылается на принцип и стандарт, но не дублирует их.
